<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Create Account</title>
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="w-full min-h-screen bg-zinc-900 text-white flex items-center justify-center">

  <!-- ✅ Create Account Card -->
  <div class="bg-zinc-800 border border-zinc-700 rounded-xl p-8 w-full max-w-md shadow-lg">
    <h1 class="text-2xl font-semibold text-center mb-6">Create Account</h1>

    <form action="/register" method="post" class="flex flex-col gap-4">
      <input class="px-3 py-2 rounded-md outline-none bg-zinc-700 placeholder-gray-300 border border-zinc-600 focus:border-blue-500" type="text" placeholder="Name" name="name" required>
      <input class="px-3 py-2 rounded-md outline-none bg-zinc-700 placeholder-gray-300 border border-zinc-600 focus:border-blue-500" type="text" placeholder="Username" name="userName" required>
      <input class="px-3 py-2 rounded-md outline-none bg-zinc-700 placeholder-gray-300 border border-zinc-600 focus:border-blue-500" type="password" placeholder="Password" name="password" required>
      <input class="px-3 py-2 rounded-md outline-none bg-zinc-700 placeholder-gray-300 border border-zinc-600 focus:border-blue-500" type="email" placeholder="Email" name="email" required>
      <input class="px-3 py-2 rounded-md outline-none bg-zinc-700 placeholder-gray-300 border border-zinc-600 focus:border-blue-500" type="number" placeholder="Age" name="age" required>
      
      <input class="px-5 py-2 rounded-md bg-blue-600 hover:bg-blue-700 cursor-pointer active:scale-95 transition border border-blue-500" type="submit" value="Create Account">
    </form>

    <p class="text-center mt-4 text-gray-400">
      Already have an account?
      <a href="/login" class="text-blue-500 hover:underline">Login</a>
    </p>
  </div>

</body>
</html>
