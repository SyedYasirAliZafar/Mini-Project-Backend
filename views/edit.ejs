<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Post</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 text-white flex flex-col items-center justify-center min-h-screen">

  <div class="bg-zinc-800 p-8 rounded-xl shadow-lg w-full max-w-lg">
    <h1 class="text-2xl font-semibold mb-6 text-center">Edit Your Post</h1>

    <form action="/edit/<%= post._id %>" method="post" class="flex flex-col gap-4">
      <textarea 
        name="content"
        class="border border-zinc-700 rounded-md bg-zinc-900 p-3 resize-none text-gray-200 focus:outline-none focus:border-blue-500"
        rows="5"
        required
      ><%= post.content %></textarea>

      <div class="flex justify-between">
        <a href="/profile" class="px-4 py-2 bg-gray-600 hover:bg-gray-700 rounded-md">Cancel</a>
        <button type="submit" class="px-4 py-2 bg-blue-600 hover:bg-blue-700 rounded-md">Save Changes</button>
      </div>
    </form>
  </div>

</body>
</html>
